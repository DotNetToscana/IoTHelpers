<Page
    x:Class="Humiture.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Humiture"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontSize="48"
    RequestedTheme="Dark"
    mc:Ignorable="d">

    <Grid Padding="50" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*" />
            <ColumnDefinition Width="50*" />
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center">
            <Image
                Height="120"
                Margin="8"
                Source="/Assets/Icons/Temperature.png" />
            <TextBlock
                x:Name="temperatureTextBox"
                Text="-"
                TextAlignment="Center" />
        </StackPanel>
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <Image
                Height="120"
                Margin="8"
                Source="/Assets/Icons/Humidity.png" />
            <TextBlock
                x:Name="humidityTextBox"
                Text="-"
                TextAlignment="Center" />
        </StackPanel>
    </Grid>
</Page>
